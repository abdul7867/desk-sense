# Build machine / Kaggle only: fine-tune, trim, export, quantize, calibrate.
# CPU torch: pip install --index-url https://download.pytorch.org/whl/cpu torch==2.14.0
-r requirements-runtime.txt
laya==0.3.6
torch==2.14.0
transformers==5.17.0
safetensors==0.8.0
huggingface_hub==1.32.0
onnx==1.23.0
onnxscript==0.7.2
pytest==9.1.1
