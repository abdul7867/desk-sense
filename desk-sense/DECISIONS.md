# Decisions

Plan §14.2 format. Newest last. Every number here was **measured** in the Day-1 dev container:
16 GB RAM, 4 vCPU, Linux 6.18, Python 3.11, no GPU. It is **not** the 4 GB target machine.
Evidence files are in `reports/`.

| Date | Decision | Why | Alternatives considered | Owner |
|---|---|---|---|---|
| 2026-09-23 | **Use case: support-ticket triage. Languages: Hindi, plus English including Hinglish.** Five questions: `department` (choice, 5), `urgency` (score, 3), `refund_requested` (noul), `sentiment` (choice, 3), `needs_human` (noul). Source of truth: `schema.json`. | The owner left the use case open. This is the plan's own sanity-check example, it covers all three question types, and the base model already routes such tickets sensibly. | Email intent; a placeholder-only schema | Sprint lead |
| 2026-09-23 | **G4/G5 thresholds set before any results:** accuracy ≥ 75% and ECE ≤ 0.10 per language; peak ≤ 500 MB and p95 ≤ 2.0 s. | Plan M15: thresholds come before results. | — | Sprint lead |
| 2026-09-23 | **Pinned by Hugging Face revision `052592a1…`, the sha256 of `model.safetensors`, and `laya==0.3.6`. The upstream GitHub fork is still to do.** | GitHub returns 403 from this environment, so the repo can't be forked here. The weights and code are pinned anyway (plan M4). | Wait for GitHub access | ML dev |
| 2026-09-23 | **G1: export the whole model, with the decision head's two attention layers replaced by export-only equivalents (same weights, same maths).** Result: worst probability difference **0.000052** over 200 requests / 609 questions (limit 0.01). Top answer matches everywhere and the tokens match exactly. **PASS** (`reports/g1_summary.json`). | `nn.MultiheadAttention`'s ONNX trace fixed the sequence length at its trace-time value. The first export only worked at 300 tokens, which is the plan's §8.2 failure mode. | Encoder-only ONNX plus a numpy head; the dynamo exporter | ML dev |
| 2026-09-23 | **int8 is weight-only:** block-32 int8 `MatMulNBits` for the encoder and head, plus our own block-32 int8 for the embedding table. The plan's `quantize_dynamic` is **rejected**. | Evidence below. The layer sweep puts the damage in `mlp.Wo` (74% agreement alone), which points at activation outliers: dynamic int8 also squeezes activations into one 8-bit range per tensor. Weight-only keeps activations in fp32. | See the table below | ML dev |
| 2026-09-23 | **Vocab trim stage A (drop tokens in scripts the guard refuses) is always on.** 256k → 197k tokens. Tokenizer file 34 → 13 MB; int8 weights 364 → 313 MB. | Lossless for any input the guard accepts: G1 still passes against the *untrimmed* torch model (0.000052), and 19/19 hand-picked hard strings (names, codes, Hinglish, emoji) tokenize the same. | — | ML dev |
| 2026-09-23 | **Stage B (lossy) trim must be corpus-based (plan §8.4). The merge-rank cap is rejected.** | At 37k tokens a rank cap brings whole-app memory to **362.5 MB**, but only **60.5%** of decisive answers still match fp32. Global merge rank isn't our tickets' distribution, and the model never saw those token splits. | Rank cap (rejected); fine-tune after trimming (breaks R6 order) | ML dev |
| 2026-09-23 | **Weights ship as ONNX external data (`model.onnx` + `model.data`).** | ORT memory-maps external data. The session costs **161 MB** RSS right after creation, against 575 MB with weights inside the protobuf. Load peak drops from 732 to 437 MB. | ORT format (1,041 MB peak: worse) | App dev |
| 2026-09-23 | **Worker memory settings:** ORT arena **on** (against plan §10.1); questions batched up to **512 tokens** (one at a time on long tickets); `malloc_trim` after tokenizer load and after every request; `MALLOC_ARENA_MAX=1` with large blocks via mmap. | Worker peak on a 512-token ticket: 5-question batch without arena 811 MB; one at a time without arena 561; **one at a time with arena 416**; 5-question batch with arena 726. Without the arena, glibc fragments on per-operation allocations. | Plan's arena-off setting (measured worse) | App dev |
| 2026-09-23 | **The watchdog reads private memory (Linux anonymous, Windows private bytes), not RSS. The limit stays 450 MB.** | Clean pages from the memory-mapped weights count in RSS but the OS can reclaim them. On RSS, a healthy worker crosses 450 MB once enough embedding rows are paged in, and gets killed in a loop. The R1 gate still uses peak RSS. | RSS watchdog (would kill healthy workers) | App dev |
| 2026-09-23 | **G2: FAIL by 2 MB in the worst measured case; stage B corpus trim is required (plan Day 5 rule).** Same bundle, same code: **397.7–398.9 MB** (5 runs), but **502.2 MB** when measured straight after the bundle file was written. | The gap is entirely file-backed weight pages (195 vs 298 MB). A freshly written file sits in the page cache in large blocks, so mapping one block maps all of it; a byte-identical rebuild reproduced 471 MB in the worker. We gate on the worst measured value. The stage B proxy measures 362.5 MB even in the freshly-written state. | Report 398 as a pass (rejected: not robust) | Sprint lead |
| 2026-09-23 | **G5 latency: FAIL expected for long tickets; decide on Day 5 with real ticket lengths.** Model p50 **0.83 s**, p95 **6.9 s** on 2 threads. The benchmark makes every 10th request a full 512-token ticket. | Laya re-reads the ticket once per question: 5 × 512 tokens is about 7 s here and will be slower on an i3. Short tickets meet 2 s. | Cap input at 256 tokens; fewer questions per call; one-question fast path | Sprint lead |
| 2026-09-23 | **Zone confidence = top calibrated probability**, not Laya's entropy-based `confidence`. | Temperature calibration makes probabilities honest; the entropy score isn't a probability, so 0.85 wouldn't mean "85% right". | Laya `confidence` | App dev |
| 2026-09-23 | **Worker talks JSON lines over stdin/stdout; the HTTP API is only on 127.0.0.1 (R7).** | Nothing else can reach the worker. A crash surfaces as end-of-file, which the supervisor turns into a replay (R8). | Local socket to the worker | App dev |
| 2026-09-23 | **CLI, not Tkinter,** for Week 1. | Plan M10 and the solo guidance. | Tkinter | App dev |
| 2026-09-23 | **We wrote our own fine-tune script** (`model/finetune/finetune.py`) on laya 0.3.6's model and sequence code. It saves checkpoints `laya.load()` can read. Smoke-tested on CPU only. | The author's notebook is on GitHub, which is unreachable here. | Wait for the notebook | ML dev |
| 2026-09-23 | **Language guard:** scripts allowed = Latin + Devanagari; up to 10% of letters may come from other scripts; the language label goes by majority script per *word*. Tamil is refused (not on our list). | Counting letters labeled "मेरा OTP नहीं आ रहा, login blocked है" as English, because Devanagari packs more sound into fewer letters. | Letter-count majority | App dev |
| 2026-09-23 | **Model input cap: kept at 512 for now; configurable via `schema.json` `model_max_len`.** Measured (100 requests, the longest tickets filling the window, 2 threads): 512 → p95 **7.0 s**, peak 398.8 MB; 256 → p95 **3.0 s**, peak 387.7 MB; p50 0.85 s for both. On the dummy tickets, 256 would refuse 6.7% as too long, 384 would refuse 2.9%, 512 would refuse 0.7% (`reports/synthetic/input_cap_fit.json`). | 256 doesn't reach G5's 2 s either, and the right cap depends on real ticket lengths. Tickets that don't fit are refused, never cut. | 256 now; 384 (latency not yet measured) | Sprint lead |
| 2026-09-23 | **Dummy (synthetic) dataset to exercise Days 2–7 before real tickets exist:** 1,200 tickets (400 each Hindi / English / Hinglish), from `model/synthetic.py`, seed 2530. All results go to `reports/synthetic/`; models go to the gitignored `model/synthetic-run/`. | You asked to keep going without waiting for data. Labels follow from how each ticket was composed, so these numbers show the pipeline works, **not** real-world accuracy. | Wait for real data | Data lead |
| 2026-09-23 | **Splits keep whole phrasings (`group`) together and spread every variety × department (`stratum`) across all four splits.** The synthetic split was redone once (`--force`) before anything had read it: grouping by department alone left 3 of 15 variety × department cells missing from the test set. | Without grouping, test tickets would share their wording with training tickets and accuracy would be inflated. | Row-level split | Data lead |
| 2026-09-23 | **Fine-tuning freezes the 256k × 768 embedding table by default.** | That table is 197M of the 322M parameters: a few hundred tickets can't improve it, and freezing it saves memory. Frozen rows stay identical to the base model, so trimming and int8 behave as already measured. | Train all parameters (`--no-freeze-embeddings`) | ML dev |
| 2026-09-23 | **G3 on dummy data: PASS for both languages, so both are kept.** Fine-tuned on 200 tickets per language for 2 epochs (CPU, 24.6 min, 7.2 GB peak): validation accuracy **en 96.7%** (baseline 54.9%), **hi 95.1%** (baseline 54.3%). Validation phrasings were never seen in training (`reports/synthetic/finetune_ckpt_g3.json`). | The fine-tune → evaluate → baseline loop works end to end. Dummy tickets are much easier than real ones, so the real G3 needs real labels. | — | ML dev |
| 2026-09-24 | **Full fine-tune on dummy data:** 743 tickets, 2 epochs, embeddings frozen, CPU 43 min, 7.1 GB peak. Validation accuracy: English 98.4%, Hindi 97.1% (the torch evaluation inside the build reads Hindi at 96.3%). | Needed a fine-tuned checkpoint to run Days 4–7. A 16 × 512-token batch was killed for running out of memory at 14 GB; micro-batches under a token budget hold ~6 GB with the same gradients. | Kaggle GPU (not reachable from here) | ML dev |
| 2026-09-24 | **The first corpus-trim build FAILED its own per-step gate (−28 points); two trim bugs fixed.** The question/option text wasn't in the corpus, and the merge closure followed only one producer per token. `trim_vocab` now refuses to write a tokenizer that changes any corpus line. | The gate did its job. Evidence: `reports/synthetic/build_attempt1_failed.*`. | — | ML dev |
| 2026-09-24 | **Vocabulary for the dry run: corpus + top 100k merges (38,317 tokens).** Every build is identical on validation (trimmed fp32 = torch; int8 −0.9/+0.4 pt). On 36 hand-written out-of-vocabulary tickets: untrimmed vocabulary en/hi/Hinglish 91.7/91.7/88.3%; **38k: 86.7/90.0/86.7%, peak 343 MB**; corpus-only 8.5k: 81.7/88.3/88.3%, peak 329 MB. The untrimmed vocabulary peaks at 398–502 MB. | The 38k vocabulary passes G2 with 157 MB spare and loses least on unfamiliar words. With real tickets the corpus grows and the gap should shrink; re-measure then. Samples are small (12 tickets per variety; 1 answer = 1.7 pt). | Untrimmed (G2 fails worst case); corpus-only (−10 pt en on unseen words) | Sprint lead |
| 2026-09-24 | **Day-7 dry run, test split read once: English (incl. Hinglish) PASS 97.3% / ECE 0.024; Hindi FAIL on ECE 0.117 (accuracy 84.7%, department 43%). Hindi would be disabled (R4).** | Hindi generalises poorly from 5 training phrasings per department. We did **not** look at test errors to tune anything (R5). Real, varied Hindi tickets are the fix. | Tune on the test errors (forbidden by R5) | Sprint lead |
| 2026-09-24 | **The evaluator scores language and variety from one pass over the test split.** | So Hinglish gets its own number without reading the locked test set twice. | Two runs (breaks "once") | ML dev |
| 2026-09-24 | **G5 stability: 1-hour soak PASS.** 2,012 requests, all done, 0 worker restarts; peak 344.7 MB, flat against the 200-request run (no leak). **G5 speed still fails for long tickets** (p95 8.0 s against 2 s); short tickets take about 1 s. | The crash-free-hour half of G5 is shown; the latency half needs the input-cap decision and real ticket lengths. | — | Sprint lead |
| 2026-09-24 | **Second use of the engine: a local, Jev-style browser agent** (`BROWSER_AGENT_PLAN.md`). The local model picks each step from a shortlist of page elements; a thinker (Claude API) plans and handles hard steps. Ticket triage is unchanged. | The owner wants Claude-in-Chrome-style work to run faster on a 4 GB PC. Laya already speaks Jev's `/v1/systemone` protocol and runs in ~345 MB here. | Cloud Jev (closed, per-call fees, no offline); Claude in Chrome alone (slow per step) | Owner |
| 2026-09-24 | **Thinker default: Claude Haiku 4.5 (`claude-haiku-4-5`); models that take an effort level run at `low`.** Configurable with `THINKER_MODEL` / `THINKER_EFFORT`. | The owner's choice: speed and cost over depth. The thinker only answers short structured questions (plan, pick, compose). | Opus 5 (the SDK guide's default: rejected by the owner) | Owner |
| 2026-09-24 | **The thinker lives in `thinker/`, outside `app/`, and runs in the supervisor process only.** The SDK is imported when first used; `requirements-browser.txt` is optional. | `app/` may only use onnxruntime/tokenizers/numpy/psutil/stdlib (`.claude/rules/backend.md`), and the worker must stay small. | SDK in the worker (rejected: RAM) | App dev |
| 2026-09-24 | **Browser steps skip SQLite-first and crash replay** (`Supervisor.decide`). A lost step is reported and the page is re-read. Steps are logged afterwards by a background writer to their own file (`data/browser_steps.db`, `synchronous=NORMAL`). | Replaying a click after a crash could act on a page that has changed. R8 protects tickets, not clicks. The log is training data, so losing one row costs one example. | Reuse `submit()` (unsafe replay, two fsyncs per step) | App dev |
| 2026-09-24 | **Browser questions refuse options that don't fit the head budget (`options_too_long`) instead of cutting them.** Tickets keep laya's cutting so G1 parity holds. The ranker keeps the shortlist to 12 elements so browser steps don't hit the limit. | Silently cut option labels would make the model choose between different, shorter labels than the ones shown. | Raise `head_max_len` (retrain, slower) | App dev |
| 2026-09-24 | **Language guard for browser tasks goes per field.** The goal must pass the ticket rule. Each element label is checked on its own; an unreadable label is shown as `[label: unsupported script]`, and if one is among the top 3 candidates the step goes to the thinker. | Web pages mix scripts freely: the ticket rule would refuse most pages. The model still never guesses on text it can't read. | Page-level guard (refuses too much); no guard (guesses) | App dev |
| 2026-09-24 | **HTTP boundary hardened for every route:** a foreign `Origin` → 403 (only the paired extension may call from a browser), a non-loopback `Host` → 403 (DNS rebinding), bodies over 256 KB → 413, and a Bearer token when configured (always for `browser.serve`). | With a browser agent attached, any web page open in the same Chrome could otherwise post to 127.0.0.1 and drive it. | Token only (does not stop rebinding) | App dev |
| 2026-09-24 | **Actions use `chrome.debugger` Input events; the page is read by an injected content script.** | Trusted events work on React inputs and on sites that ignore scripted clicks (bench `flights` checks `isTrusted`). The cost is Chrome's "debugging" bar while a task runs. | DOM `.click()` / synthetic events (fails on such sites) | App dev |
| 2026-09-24 | **A step counts as done only when its own target was acted on.** | Bench `checkout-stops` failed once: a no-op "Add to cart" click (change detection compared text length, "Cart: 0"→"Cart: 1") led to a fallback click on "Checkout" that was counted as finishing "Add to cart". Change detection now hashes text and form state. | — | App dev |
| 2026-09-24 | **Secret field values are masked twice**: the extension reports passwords, card fields (`cc-*`), one-time codes and ID-number fields as `(filled)`, and the engine re-masks with `safety.redact` before the model, the thinker or the step log see a page. Dropdown choices get the same risky-word and secret-field confirmation as clicks and typing. | The code review found an already-typed card number could reach the Claude API when the thinker was consulted on the same page, and a "Delete selected" bulk action ran without confirmation. Both now have failing-before tests. | Mask in the extension only (one missed label leaks) | App dev |
| 2026-09-24 | **B0 baseline measured** (base Laya int8, stage A trim, untrained on browser steps; 500 generated steps of 30–150 elements, shortlist 12; 2 threads): step **p50 520 ms, p95 583 ms**, ~248 tokens per step, engine peak **389 MB**, 0 restarts, 0 refusals, no torch in the worker. Threads: 1 → 964 ms, 4 → 324 ms p50. Untrained zones: 77% flag, 20% confirm, 3% act (`reports/browser/b0_*.json`). | The plan's first gate: real numbers before any speed work. The rebuilt bundle reproduces Day-1 G1 exactly (0.000052). | — | Sprint lead |
| 2026-09-24 | **Fixed moves reworded to the shortest labels** ("scroll down", "wait", "step already done", "none fits"). **Shortlist stays at 12** until B2 recall is measured. | Latency tracks tokens (~2 ms/token on 2 threads): short fixed moves 248 → 233 tokens (−5%); 8 candidates → 203 tokens (−16%); both → 188 tokens, 414 ms p50. Wording is cheapest to change before any training; cutting candidates can drop the right element, so it waits for data. | 8 candidates now (unmeasured recall) | App dev |
| 2026-09-24 | **int8 compute (`accuracy_level=4`) measured, not adopted yet; 4-bit weights rejected on the base model.** On the same 200 steps: int8 compute 432 ms p50 (−17%), same pick 99.5%, decisive 127/127, but worker peak **+66 MB** (417 MB). 4-bit weights 388 ms (−26%), peak unchanged, but same pick only 83%, decisive **112/127** (fails the 98% rule). Best combination (int8 compute + short moves + 8 candidates): **312 ms p50 / 346 ms p95 at 448 MB** on 2 threads; 190 ms on 4 threads. | B4 wants p50 ≤ 300, p95 ≤ 500 and ≤ 400 MB together: the base model can reach the speed or the memory, not both. That is the case for the distilled student (B5), as planned. Re-check 4-bit after fine-tuning. | Ship int8 compute now (breaks the 400 MB target) | Sprint lead |
| 2026-09-24 | **Real-model benchmark:** exact-label suite **15/15** (fast paths do every step; the model isn't asked). Fuzzy suite (plans worded unlike the labels) **9/15**: the untrained model flags almost every step, the scripted thinker takes over, and the 5–6-step tasks run out of the 4-call resolve budget and stop as blocked. No wrong risky action, "Pay now" stopped every time (`reports/browser/bench_real*.json`). | Shows the safe failure mode before training: an unsure model hands off, and the budget stops cost from running away. The fuzzy suite is the B3 accuracy check to re-run after fine-tuning. | Raise the budget (hides the untrained model) | Sprint lead |
| 2026-09-24 | **Browser training data: Mind2Web train split (CC-BY-4.0; the authors ask for research use), converted to runtime questions.** 1,009 tasks → 7,087 steps (688 dropped: target not found or nameless). The planner's wording is not in Mind2Web, so each step gets a seeded wording: exact label, partial label, or none. 90% of "none" steps whose target falls outside the shortlist are dropped (they only teach "none fits"). Split by **website**: train 3,892 steps / 52 sites; val 459 / 7; calib 572 / 7; test 530 / 7 (locked). Raw data stays in gitignored `data/`. **The owner must confirm the licence fits before a model trained on it ships.** | Real page structure from 73 sites, free and downloadable here. Splitting by site means validation and test measure pages the model has never seen. | Wait for the owner's own traces (none yet); Claude-labelled steps (no API key here) | Data lead |
| 2026-09-24 | **B2 shortlist recall, measured on Mind2Web (7,087 steps, 30–600 candidates, median 418):** right element in the top 12 on **99.2%** of steps with the exact label, **96.0%** with a partial label, **17%** with no target words. Planned steps (exact + partial) together: about **97.4%**, right at the 97% gate. | Our planner always names a target, so the with-target figure is the one that applies. Steps with no target need a better ranker (goal words) or the planner. | Shortlist 24 (more tokens, slower) | Sprint lead |
| 2026-09-24 | **Bug fixed: the stage-A vocabulary trim was not lossless. It dropped every non-special added token, including "\n", "\n\n", tabs and runs of spaces** (91 tokens). At run time every newline became a byte token and the next word lost its word-start marker, so multi-line text read differently than in training. Now every added token whose letters are in allowed scripts is kept (lossless on newline, tab, space-run and Hindi samples; vocabulary 197k either way). G1 gained two multi-line cases; a regression test covers the trim. | Found by the browser build's own per-step gate: the ONNX model disagreed with torch on 51/459 browser steps (whose states are multi-line) while G1 passed, because no G1 case had a newline. **Ticket impact:** the earlier ticket numbers came from single-line synthetic tickets and are not expected to move, but pasted multi-line emails were affected; the ticket bundle is rebuilt and re-gated. | — | ML dev |
| 2026-09-24 | **Second code review: four fixes.** (1) The Mind2Web converter now names elements in the extension's order (aria-label, label, input button value, then placeholder, text, title, alt). The old order changed the question text on **1,415 of 7,074** steps (20%), a train/serve skew, so the browser model is retrained on re-converted data. (2) The "drop 90% of blind none-fits steps" filter moved from an inline script into `model/browser/split_browser.py`; it reproduces the existing splits byte for byte. (3) `majority_baseline` refuses to score tickets and browser steps mixed in one set. (4) Three different detours in a row now bring in the thinker, not only an exact repeat. | Review of the diff since the last review. A target ranked 13–24 stays labelled "none fits": that answer is what makes the controller show the next 12, so it is the right label. | Keep the first checkpoint (trained on skewed names) | App dev |
| 2026-09-24 | **Gate B3 (browser model, unseen sites): PASS with a browser act bar of 0.95** (`schema_browser.json`; tickets keep 0.85). Checkpoint 2: re-converted names, duplicate labels removed, 2 epochs, max length 320 (CPU, 104 min). Val (7 sites, 470 steps): accuracy **82.3%** vs 69.6% for the ranker's first choice; act precision **97.7%** at coverage **54.7%**; ECE **0.056**. Calib (7 other sites, 564 steps), same bar: precision **97.8%**, coverage **55.3%**, ECE 0.033. Build gate: torch = ONNX = 82.6%, int8 82.3%. Checkpoint 1 for comparison: 73.9%, act precision 92.0% at the 0.85 bar. The test split is still unread. | At 0.85 precision was 95.6%, short of the 97% gate. The threshold was chosen on val and confirmed on calib, whose sites played no part in choosing it. A wrong click costs more than a misrouted ticket, hence the higher bar. | 0.93 (97.0–97.1%: no margin); retrain for precision (slower) | Sprint lead |
| 2026-09-24 | **Real-Chrome fuzzy suite with the trained model: 12/15** (was 9/15 untrained). The model now settles steps itself (contact form: 4 of 7 steps); flights still stops at the thinker budget (autocomplete steps stay unsure). Speed and memory are unchanged by training: **507 ms** p50 per step on 2 threads, **389 MB** (`reports/browser/b4_trained_ckpt2.json`). | Training changes what the model knows, not its cost. Speed (B4/B5) still needs the distilled student. | — | Sprint lead |
| 2026-09-24 | **Gate B1 (plumbing): PASS.** Real extension in headless Chromium, fake model and scripted thinker: **15/15** (5 offline tasks × 3 runs), median **0.90 s** per task, "Pay now" stopped every time, 0 untrusted clicks (`reports/browser/bench_fake.json`). | Shows the loop, the safety stop and trusted input work end to end. It says nothing about model accuracy or model latency: the plans name exact labels, so fast paths did nearly all the work. B0/B3–B5 need a model build. | — | Sprint lead |

## int8 evidence (drift against the ONNX fp32 model; 60–200 generated requests)

"Decisive" means answers where fp32's top-two margin is at least 0.2, the ones the act zone relies on.

| Method | Weights | Agreement (all) | Agreement (decisive) | Worst Δp |
|---|---|---|---|---|
| `quantize_dynamic`, per-tensor | 323 MB | 72.4% | 79.7% | 0.94 |
| `quantize_dynamic`, per-channel | 324 MB | 72.0% | — | 0.96 |
| weight-only int8, embeddings fp32 | 929 MB | 99.0% | 100% | 0.08 |
| weight-only int8, embeddings int8 per row | 340 MB | 97.7% | 98.6% | 0.12 |
| **weight-only int8, embeddings int8 block 32 (chosen)** | **364 → 313 MB after trim** | **99.0%** | **100%** (478/478) | 0.26 |
| same, `accuracy_level=4` (int8 compute) | 364 MB | 97.4% | 98.9% | 0.25 |

Layer sweep with `quantize_dynamic`, one group at a time (`reports/quant_sweep.json`): mlp.Wo 74.4%,
attn.Wqkv 92.6%, mlp.Wi 95.5%, embeddings 98.3%, attn.Wo 99.4%, decision head 100%.
